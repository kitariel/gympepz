# Go-Train

## Cypress

### Setup
- Install deps: `pnpm install`
- Set env:
  - `CYPRESS_TEST_EMAIL`
  - `CYPRESS_TEST_PASSWORD`
  - Optional: `CYPRESS_BASE_URL` (defaults to `http://localhost:3000`)

### Run
- UI: `pnpm cypress:open`
- Headless: `pnpm cypress:run`
